import "./hired-list.styles.scss";

export const HiredList = ({ users }) => {
  return (
    <div className="hired-container">
      <p className="text">TESTING</p>
      {/* <p className="text">{isHired}</p> */}
      {/* {users.map((user) => {
        // return <Card key={user.id} user={user} />;
        // <div>user</div>;
      })} */}
    </div>
  );
};

export default HiredList;
